# 从一个想法到一个能运行的小程序：我的 AI 协作开发实操笔记

> 项目：豆包视频链接解析与图片水印区域修复小程序  
> 适合读者：没有完整开发经验，但愿意按步骤操作的人  
> 仓库：[liyuan-ohh/Doubao-watermark-remover](https://github.com/liyuan-ohh/Doubao-watermark-remover)  
> 文档版本：2026-07-31

---

## 写在前面

这篇笔记不是一篇“让你复制几段代码就假装学会了”的教程，而是我从提出一个模糊想法开始，怎样与 AI 一起把它整理成需求、做成微信小程序、发现问题、定位问题、验证结果，再准备把它部署到公网的完整过程。

我一开始的想法其实很简单：

> 我想做一个微信小程序。别人把豆包生成视频的分享链接发给我以后，我粘贴链接，就能解析并保存没有动态水印的视频；另外还想支持上传图片，手动框选水印位置后进行修复。

真正开始做以后，我才发现“做一个去水印小程序”至少包含四件不同的事情：

1. 把想法说清楚，决定第一版到底做什么。
2. 做出小程序页面，也就是用户能看到和点击的部分。
3. 做出后端服务，也就是实际解析链接和处理图片的部分。
4. 验证结果确实有效，而不是页面上写着“解析成功”就算成功。

本文会把这四件事逐步拆开。

> **使用边界：**请只处理自己拥有或已经获得授权的图片、视频，并遵守平台规则和当地法律。本文讨论的是个人学习和授权素材处理，不代表可以随意处理或传播他人的作品。

---

## 一、最终做出了什么

第一阶段已经做出一个原生微信小程序，包含两个核心模式。

### 1. 豆包链接解析

用户可以：

1. 从豆包复制公开的视频分享链接或完整分享文案。
2. 粘贴到小程序。
3. 点击“一键解析”。
4. 预览解析结果。
5. 保存视频。

这条流程不要求最终用户登录豆包。

### 2. 图片水印区域修复

用户可以：

1. 从相册选择一张 JPEG、PNG 或 WebP 图片。
2. 在图片上拖动，框选需要修复的位置。
3. 点击修复。
4. 预览处理结果。
5. 保存图片。

图片功能不是“自动识别所有水印”，而是让用户自己框选区域，再由 OpenCV 根据周围像素进行填补。背景越简单，效果通常越自然；人脸、文字和复杂纹理不能保证完美恢复。

### 3. 当前真实状态

请特别注意，项目现在分成两个阶段：

- **已经完成并验证：**在本地电脑启动后端，在微信开发者工具里完成视频解析和图片修复。
- **正在进行：**把后端部署到微信云开发，让其他人不连接我的电脑也能使用。

所以，本教程中的本地运行部分可以直接照做；公网部署部分目前记录的是已经确定的方案和开通步骤，后续还要继续完成代码改造、云端部署和真机验收。

---

## 二、小白先理解三个词：前端、后端、云端

在操作前，先用最简单的话理解这个项目。

```text
用户在微信小程序里点击按钮
            ↓
小程序把链接或图片发给后端
            ↓
后端解析视频，或者修复图片
            ↓
后端把结果交回小程序
            ↓
用户预览并保存结果
```

### 1. 前端是什么

前端就是用户在微信里看到的页面。这个项目的前端代码主要放在：

```text
pages/index/
```

它负责输入框、按钮、模式切换、图片框选、视频预览和保存操作。

### 2. 后端是什么

后端是一段运行在电脑或服务器上的程序。这个项目使用 Python、FastAPI 和 OpenCV，主要代码放在：

```text
backend/
```

它负责：

- 检查输入是不是允许的豆包公开分享链接。
- 请求解析服务并校验返回的是无水印地址。
- 接收图片和框选坐标。
- 使用 OpenCV 修复框选区域。

### 3. 云端是什么

本地开发时，后端只运行在我的电脑上：

```text
http://127.0.0.1:8000
```

`127.0.0.1` 的意思是“当前这台电脑自己”。其他人的手机不能访问我电脑里的这个地址。

要让别人使用，就要把后端放到一直在线的云服务中。这个项目下一阶段计划使用微信云开发 CloudBase 云托管。

---

## 三、我是怎样把模糊想法变成清楚需求的

### 第 1 步：先只说结果，不急着说技术

一开始不需要知道 FastAPI、OpenCV 或云托管。先告诉 AI，用户最终要完成什么。

我可以这样说：

```text
我想制作一个微信小程序，重点完成两个功能：

1. 用户粘贴豆包公开分享文案或链接，解析出无动态水印的视频，支持预览和保存。
2. 用户上传图片，手动框选水印区域，修复后预览和保存。

第一版只做这两个核心功能，不做广告、账号、历史记录、付费和后台管理。
```

这一步最重要的不是“提示词写得高级”，而是把核心结果和不做的范围一起说明。

### 第 2 步：给 AI 看参考图，但告诉它参考什么

我提供了自己常用工具的截图。截图中有广告、教程、历史记录和“更多”页面，但这些并不是我要复制的内容。

所以我补充了这样的要求：

```text
参考截图的功能布局和操作路径，但不要照搬广告、推广、历史记录、教程和更多页面。
页面重点只保留“链接解析”和“图片去水印”两个入口。
```

这一步的经验是：**参考图不等于需求。**如果不明确告诉 AI 哪些要、哪些不要，它很容易把截图里所有东西都当成产品范围。

### 第 3 步：先让 AI 写 PRD，再开始写代码

PRD 可以理解为“产品说明书”。第一版 PRD 至少应该回答下面的问题：

| 问题 | 本项目的答案 |
| --- | --- |
| 给谁用 | 第一阶段先给开发者本人使用，第二阶段再给普通用户 |
| 解决什么问题 | 分享链接解析、图片框选修复 |
| 核心操作 | 粘贴、解析、预览、保存；选图、框选、修复、保存 |
| 成功是什么 | 得到真实无动态水印视频，图片框内像素得到修复 |
| 失败怎么办 | 保留输入和框选结果，显示易懂提示，允许重试 |
| 第一版不做什么 | 广告、账户、历史、批量、自动识别、付费、后台 |

我给 AI 的 PRD 请求可以这样写：

```text
请先不要写代码。把上面的想法整理成第一阶段 PRD，至少包含：

- 产品目标
- 目标用户
- 两条核心用户故事
- 页面和交互
- 输入限制与错误提示
- 明确不做的功能
- 可逐项检查的验收标准

所有功能都以“能否验收”为准，不要添加我没要求的功能。
```

### 第 4 步：先研究可复用方案，不急着从零造轮子

我让 AI 去 GitHub 搜索公开项目和现有思路，重点观察：

- 分享链接是怎样被提取的。
- 哪个字段或接口被当作视频源。
- 开源许可证是否允许复用。
- 项目是否仍然可用。
- 是否依赖登录 Cookie、浏览器环境或作者自己的服务器。

这里要记住：**GitHub 上有代码，不等于代码现在还能用，也不等于可以直接复制。**

检查开源项目时至少看三件事：

1. `LICENSE`：允许怎样使用。
2. 最近更新时间和 Issues：接口是否已经失效。
3. 真实链接测试：项目说“无水印”，结果是不是真的无水印。

### 第 5 步：把开发任务写成可以验收的任务单

我不是只对 AI 说“帮我做完”，而是尽量把任务写成下面的结构：

```text
角色：你负责实现，主 Agent 负责产品验收。
工作目录：给出唯一、完整的项目路径。
允许修改：列出可以改的文件和功能。
禁止修改：不加广告、不做账号、不改其他项目、不做无关重构。
成功标准：真实链接解析成功，动态水印消失；图片尺寸保持不变。
失败标准：非法链接、超时、带水印结果必须明确失败。
回报内容：修改了哪些文件、跑了什么测试、哪些项目尚未验证。
```

这样做可以明显减少 AI 改错目录、擅自扩展功能、只说“应该可以”却没有验证的问题。

---

## 四、准备本地开发环境

下面以 Windows 和 PowerShell 为例。

### 1. 需要准备的软件和账号

开始前需要：

- 一台 Windows 电脑。
- Git。
- Python 3.10 或更高版本。本项目实际验证环境是 Python 3.12.3。
- 微信开发者工具。
- 一个微信小程序 AppID；只看界面时也可先使用测试号，云开发和真机功能需要自己的 AppID。
- 能正常访问 GitHub 和 Python 软件包源的网络。

先打开 PowerShell，逐条检查：

```powershell
git --version
python --version
```

如果电脑使用的是 Python 启动器，也可以执行：

```powershell
py --version
```

只要能看到版本号，就说明对应命令已经安装并加入 PATH。若提示“不是内部或外部命令”，先安装对应软件，再重新打开 PowerShell。

### 2. 下载项目

先切换到自己准备存放代码的目录。例如：

```powershell
Set-Location "D:\Code"
```

克隆公开仓库：

```powershell
git clone https://github.com/liyuan-ohh/Doubao-watermark-remover.git
Set-Location ".\Doubao-watermark-remover"
```

> 如果你的路径中有空格或中文，PowerShell 命令里要用英文双引号把整个路径包起来。

### 3. 认识项目目录

下载后，项目大致是这样：

```text
Doubao-watermark-remover/
├─ backend/                 Python 后端
│  ├─ app.py                API、视频解析、图片修复
│  ├─ requirements.txt      Python 依赖
│  ├─ test_app.py           自动检查
│  └─ THIRD_PARTY_NOTICES.md
├─ pages/index/             小程序主页面
├─ components/              小程序组件
├─ app.js
├─ app.json
├─ app.wxss
├─ project.config.json      微信开发者工具项目配置
└─ README.md
```

微信开发者工具应该打开这一层，也就是能同时看到 `backend`、`pages`、`app.js` 和 `project.config.json` 的目录。

---

## 五、第一次启动本地后端

### 第 1 步：创建独立 Python 环境

在项目根目录执行：

```powershell
python -m venv backend\.venv
```

如果你的电脑只能使用 `py`，就执行：

```powershell
py -3.12 -m venv backend\.venv
```

`.venv` 是这个项目自己的 Python 环境。以后安装依赖时不会把其他 Python 项目弄乱。

### 第 2 步：安装依赖

执行：

```powershell
.\backend\.venv\Scripts\python.exe -m pip install -r backend\requirements.txt
```

需要安装的核心依赖只有：

- FastAPI：提供后端接口。
- Uvicorn：启动 FastAPI。
- httpx：请求远程解析服务。
- OpenCV 和 NumPy：处理图片。
- python-multipart：接收上传图片。

安装过程中出现黄色提示不一定是失败；最后如果出现红色 `ERROR`，才需要查看具体报错。

### 第 3 步：启动后端

在项目根目录执行：

```powershell
.\backend\.venv\Scripts\python.exe -m uvicorn backend.app:app --host 127.0.0.1 --port 8000
```

正常情况下会看到类似：

```text
Uvicorn running on http://127.0.0.1:8000
```

这个 PowerShell 窗口必须保持打开。关闭窗口，后端就停止了。

### 第 4 步：确认后端真的启动了

用浏览器打开：

```text
http://127.0.0.1:8000/docs
```

如果能看到 FastAPI 自动生成的接口文档，说明后端已经启动。

如果浏览器提示无法访问，先不要反复点击小程序，回到 PowerShell 查看是否有红色报错。

### 第 5 步：运行自动检查

另开一个 PowerShell 窗口，进入项目根目录后执行：

```powershell
.\backend\.venv\Scripts\python.exe -m unittest -v backend.test_app
```

当前项目会运行 5 项检查，覆盖：

- 拒绝非豆包链接。
- 接受合规的无水印地址。
- 拒绝缺失或带水印的地址。
- 正确提示上游超时。
- 图片框选边界、尺寸和像素变化。

看到下面的结尾才算自动检查通过：

```text
Ran 5 tests
OK
```

自动测试通过不等于最终功能一定可用。真实第三方接口、视频内容和微信保存权限仍要手工验证。

---

## 六、在微信开发者工具中打开项目

### 第 1 步：导入正确目录

打开微信开发者工具，选择“导入项目”，项目目录选择刚才克隆下来的项目根目录。

正确打开后，左侧资源管理器里应该同时看到：

```text
backend
components
pages
app.js
app.json
project.config.json
```

如果只看到一个默认页面，模拟器中间写着 `Weixin`，通常说明打开了微信示例项目或错误目录。不要继续改代码，先重新导入正确目录。

### 第 2 步：本地调试时关闭合法域名校验

因为当前后端地址是：

```text
http://127.0.0.1:8000
```

它不是正式小程序的 HTTPS 合法域名。本地开发时，在微信开发者工具中找到：

```text
详情 → 本地设置 → 不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书
```

勾选这一项，然后重新编译。

这个设置只适合本地开发。正式发布时不能靠关闭校验解决，必须使用云服务或已配置的 HTTPS 域名。

### 第 3 步：看到正确首页

正常页面顶部会显示“豆包去水印”，下面有两个切换入口：

- 豆包链接
- 图片去水印

页面没有广告、账号、历史记录和“更多”页面，因为这些不属于第一版核心目标。

---

## 七、实际测试豆包视频解析

### 第 1 步：准备分享链接

在豆包中找到自己有权处理的视频，使用分享功能复制链接。链接通常类似：

```text
https://www.doubao.com/video-sharing?...
```

也可以粘贴包含这个链接的完整分享文案。后端会从文案中提取第一个网址。

目前只允许：

- `doubao.com` 及其子域名。
- `/video-sharing` 路径。
- `/thread/` 路径。

其他域名会被拒绝，这是为了防止后端被当成任意网址请求工具。

### 第 2 步：粘贴并解析

1. 打开“豆包链接”模式。
2. 点击“粘贴”，或者手动输入分享文案。
3. 点击“一键解析”。
4. 等待按钮恢复可点击状态。

成功后会出现视频预览和保存按钮。失败时输入内容会被保留，可以直接重试。

### 第 3 步：不要只看“解析成功”四个字

豆包的视频水印是动态的，可能从右下角移动到左上角。所以只看视频开头一帧，不能证明水印已经去掉。

正确验收方法：

1. 播放完整视频。
2. 至少检查前段、中段和后段。
3. 对 15 秒左右的视频，可重点看第 2、6、10、14 秒。
4. 检查画面不同角落是否出现“豆包 AI 生成”字样。
5. 再保存到本地，播放实际下载文件进行确认。

这次项目就是在多时间点抽查后，才确认动态水印消失。

### 第 4 步：理解“保存成功”的范围

微信开发者工具里的下载成功，不完全等于真实手机保存到相册成功。真机还会受到：

- 相册权限。
- 下载域名配置。
- HTTPS 证书。
- 视频文件大小。
- 第三方视频地址有效期。

因此本地阶段的成功标准是“开发者工具中能解析、预览和走完下载流程”；正式上线前还要在真实手机上重新验收。

---

## 八、实际测试图片水印区域修复

### 第 1 步：选择图片

切换到“图片去水印”，选择一张 JPEG、PNG 或 WebP 图片。

当前限制：

- 文件不能超过 15MB。
- 总像素不能超过 2500 万。
- 图片必须能够被正常解码。

### 第 2 步：框选区域

在图片上按住并拖动，尽量只框住需要修复的内容。

框得太大会把周围正常画面一起参与修复；框得太小又可能遗漏水印边缘。建议比水印实际边缘稍微多留一点空间。

框选区域换算到原图后至少需要 `8×8` 像素，并且不能超出图片边界。

### 第 3 步：点击修复

后端会创建一张遮罩，只把框选区域交给 OpenCV 的 `INPAINT_TELEA` 算法处理。

这个算法不是“找回被水印挡住的原始内容”。原始像素已经被覆盖，算法只能根据周围颜色和纹理推测怎样填补，所以：

- 纯色背景、天空、墙面等通常较自然。
- 复杂文字、头发、五官和密集纹理可能出现涂抹感。
- 修复效果不好时，可以缩小或移动框选区域后重试。

### 第 4 步：检查结果

至少确认：

- 输出图片宽高与原图一致。
- 框选区域确实发生变化。
- 远离框选区域的内容没有变化。
- 保存按钮能够正常执行。

---

## 九、这次最关键的错误，以及怎样解决

### 问题 1：开发者工具一直显示空白或默认 Weixin

**表面现象：**重新编译也看不到做好的页面。

**真实原因：**打开了错误的项目目录。

**解决：**重新导入项目根目录，并确认左侧同时存在 `backend`、`pages`、`app.js` 和 `project.config.json`。

**经验：**遇到“代码明明有，页面却不变”，先确认自己打开的是不是同一个项目，不要急着重写页面。

### 问题 2：接口说是“原视频”，画面仍有动态水印

**表面现象：**解析成功，也能播放，但水印仍会在画面角落移动。

**真实原因：**旧方案读取的 `original_media_info.main_url` 只是豆包返回的“原视频字段”，对应 CDN 文件已经把动态水印写进像素。字段名叫 original，不代表文件一定无水印。

**解决：**不再把字段名当作证据，改用能够返回另一份无水印媒体源的解析服务，并在后端严格校验结果。

**经验：**接口字段名、页面提示和真实文件内容是三回事。最终必须看视频本身。

### 问题 3：把 URL 参数改成 no-watermark 仍然没用

**尝试：**把链接中的参数替换为 `video_gen_no_watermark` 或 `unwatermarked`。

**验证结果：**修改前后下载文件的大小和 SHA-256 完全相同，说明拿到的仍然是同一个文件。

**结论：**不能靠修改一个字符串“变出”无水印视频，必须取得真实的另一份媒体对象。

### 问题 4：为什么有些开源方案要求登录豆包

部分脚本依赖已登录网页中的 Cookie，再读取内部接口或解密地址。这里的登录状态属于解析程序维护者的后台条件，不应该转嫁给最终用户。

本项目最终选择的使用体验是：用户只提供公开分享链接，不需要自己登录豆包。

### 问题 5：已经改了代码，重新编译后还是旧水印

**表面现象：**微信开发者工具已经重新编译，但解析结果没有变化。

**真实原因：**重新编译只更新小程序前端，不会重启端口 8000 上独立运行的 Python 后端。旧进程仍然加载旧代码。

**解决：**回到启动后端的 PowerShell，按 `Ctrl+C` 停止，再重新执行启动命令。

```powershell
.\backend\.venv\Scripts\python.exe -m uvicorn backend.app:app --host 127.0.0.1 --port 8000
```

然后回到微信开发者工具，重新点击“一键解析”。

**经验：**前端和后端是两个进程。改了哪一边，就要确认哪一边真正重新加载了代码。

### 问题 6：端口 8000 已经被占用

先检查谁在监听 8000：

```powershell
$listener = Get-NetTCPConnection -LocalPort 8000 -State Listen -ErrorAction SilentlyContinue | Select-Object -First 1
$listener
```

再查看对应进程：

```powershell
Get-CimInstance Win32_Process -Filter "ProcessId = $($listener.OwningProcess)" | Select-Object ProcessId, Name, CommandLine
```

只有确认它就是本项目的 Uvicorn 后端时，才停止：

```powershell
Stop-Process -Id $listener.OwningProcess
```

不要看到端口占用就随便结束不认识的系统进程。

---

## 十、最终方案为什么能工作

现在的视频解析流程被缩短为一条清楚的链路：

```text
分享文案或链接
    ↓
提取其中的网址
    ↓
校验必须是 HTTPS、doubao.com、允许的分享路径
    ↓
请求第三方 BugPk 解析服务
    ↓
校验返回状态为 200
    ↓
校验视频地址是 HTTPS
    ↓
校验地址参数为 lr=unwatermarked
    ↓
拒绝包含 video_gen_watermark 的结果
    ↓
返回给小程序预览
```

这里有一个很重要的设计原则：

> 如果没有取得可信的无水印源，就直接提示失败，绝不能退回带水印视频后仍显示“解析成功”。

为了减少故障点，最终还删除了旧的豆包网页读取、VID 提取和元数据接口请求。封面、尺寸等信息不是第一版的核心成功条件，不应该因为它们失败而拖累视频解析。

---

## 十一、怎样让 AI 真正帮我开发，而不是只生成一堆代码

### 1. 一个可直接复制的开发任务模板

以后做类似产品，可以把下面模板发给 AI：

```text
【目标】
我要做一个什么产品，用户最终要完成什么。

【工作目录】
只允许在这个绝对路径中工作：<项目完整路径>。

【第一版范围】
必须完成：
1. ...
2. ...

明确不做：
1. ...
2. ...

【现有证据】
当前页面、接口或报错是什么；我已经怎样复现。

【实施要求】
先读取现有代码和调用链，再给出最小改动。
优先复用现有代码和原生能力，不新增不必要依赖。
只修改与本任务直接相关的文件。

【验收标准】
1. 正常输入得到什么结果。
2. 非法输入怎样提示。
3. 网络超时怎样提示。
4. 哪些旧功能不能被破坏。
5. 必须运行哪些自动测试和真实测试。

【回报格式】
列出修改文件、测试命令、测试结果和仍未验证的项目。
不要把静态检查说成真实功能已经成功。
```

### 2. 一个可直接复制的故障排查模板

```text
现在先不要修改代码，请先定位根因。

现象：<我看到什么>
预期：<我希望看到什么>
复现步骤：<第 1、2、3 步>
截图或报错：<粘贴完整内容>

请检查：
1. 实际运行的是哪个项目和哪个进程。
2. 请求实际到了哪个接口。
3. 返回地址和下载文件是否真的发生变化。
4. 是否只是前端缓存或后端旧进程。

找到证据后再提出最小修复，并给出修复后的验证步骤。
```

### 3. 一个可直接复制的验收模板

```text
请不要只报告“测试通过”。请按下面清单验收：

- 自动测试的实际命令和完整结果。
- 使用一条当前有效的真实链接进行请求。
- 检查视频多个时间点，而不是只看第一帧。
- 对比修改前后文件大小或哈希，确认不是同一个文件。
- 在微信开发者工具中检查加载、成功、失败和重试状态。
- 明确哪些只在开发者工具验证，哪些已经在真机验证。
```

### 4. 我学到的 AI 协作原则

- 先让 AI 复述需求和边界，再让它动手。
- 路径一定写完整，尤其电脑里有多个相似项目时。
- “成功”必须有可观察证据。
- 真实接口问题一定用真实链接验证。
- 修改后要检查实际运行的进程是否重启。
- AI 可以写代码，但产品范围、付费、账号和最终验收仍由人决定。

---

## 十二、怎样把代码保存到 GitHub

这一部分不是运行项目的必要条件，但可以保存版本、方便换电脑和继续让 AI 协作。

### 1. 不应该上传的文件

项目的 `.gitignore` 已排除：

```text
backend/.venv/
backend/__pycache__/
backend/outputs/
project.private.config.json
```

这些分别是本机 Python 环境、缓存、处理结果和本地私有配置，不应该上传。

尤其不要上传：

- 微信 AppSecret。
- GitHub Token。
- 云账号密码。
- 支付信息。
- 用户上传的原始图片和视频。

### 2. 使用 GitHub CLI 创建公开仓库

安装并登录 GitHub CLI 后，在项目根目录执行：

```powershell
gh auth login
gh repo create Doubao-watermark-remover --public --source . --remote origin --push
```

如果仓库已经存在，只需正常提交和推送：

```powershell
git status
git add .
git commit -m "更新说明"
git push
```

执行 `git add .` 前一定先运行 `git status`，确认没有把密码、Token、用户素材或大型临时文件加入提交。

---

## 十三、为什么别人现在还不能直接使用

本地版本的前端代码目前固定访问：

```javascript
const API_BASE = "http://127.0.0.1:8000";
```

这意味着：

- 在我的电脑上启动后端后，我的开发者工具能访问。
- 别人的手机访问 `127.0.0.1` 时，访问的是他自己的手机，不是我的电脑。
- 我的电脑关机或后端窗口关闭，服务就不存在。

所以，把代码上传 GitHub并不等于把服务部署上线。GitHub 保存的是代码，云服务负责真正运行后端。

---

## 十四、开通微信云开发时怎样选择

> 这一节记录的是当前开通页面。套餐和价格可能调整，付款前以你自己页面显示的内容为准。

在微信开发者工具中进入云开发，创建环境时，我选择的填写方式是：

### 1. 环境名称

填写：

```text
doubao-remover-prod
```

名称只使用小写字母、数字和连字符，方便以后识别这是正式环境。

环境创建后还会生成一个“环境 ID”。环境名称和环境 ID 不是一回事，后续代码接入时要使用控制台显示的真实环境 ID，不要自己猜。

### 2. 套餐

第一次验证建议选择：

```text
单月 ¥19.9
```

先不选择连续订阅，避免忘记取消自动续费。等公网功能完整跑通并确认长期使用后，再考虑连续订阅。

### 3. 超额按量付费

第一次开通建议关闭：

```text
使用超出配额部分按量付费
```

这样做是为了先控制成本。如果将来确实有真实用户量，再根据请求数、存储和流量监控决定是否打开。

### 4. 其他选项

- 代金券：没有就不选择。
- 支付方式：保持页面提供的腾讯云账户扣款方式。
- 服务条款：阅读后由账号本人勾选。
- 最终付款：必须由账号本人确认和完成，AI 不应该替人确认付费。

### 5. 开通后先记录什么

创建成功后先记录：

```text
环境名称：doubao-remover-prod
环境 ID：控制台实际生成的值
```

环境 ID 可以提供给负责修改项目代码的开发者或 AI，但不要发送 AppSecret、密码、验证码和付款信息。

---

## 十五、公网版本接下来要做什么

这一部分尚未完成，不能把它写成“已经上线”。下一阶段应该按下面顺序推进。

### 第 1 步：把 FastAPI 部署到 CloudBase 云托管

现有后端需要增加最小的容器部署文件，并满足两个要求：

- 服务监听 `0.0.0.0` 和平台提供的 `PORT`。
- 服务不能把本地磁盘当作永久存储。

CloudBase 官方文档说明，从源代码部署云托管时需要 `Dockerfile`，FastAPI 可以作为容器服务运行：

- [FastAPI 部署说明](https://docs.cloudbase.net/run/develop/languages-frameworks/fastapi)
- [从源代码部署](https://docs.cloudbase.net/run/deploy/deploy/deploying-source-code)

### 第 2 步：让小程序调用云端接口

小程序在 `app.js` 中初始化云开发，再通过 `wx.cloud.callContainer` 调用已关联环境里的云托管服务。

官方说明见：

- [微信小程序访问云托管](https://docs.cloudbase.net/run/develop/access/mini)

需要提前知道两项真实信息：

```text
云开发环境 ID
云托管服务名称
```

在拿到真实值以前，不应把占位字符串写进正式代码并假装已经完成。

### 第 3 步：解决视频预览和下载域名

当前解析服务返回的第三方 CDN 地址可能变化。正式小程序不能把不断变化的域名一个个加入白名单。

更稳定的做法是：

```text
第三方视频源 → 自己的云托管服务转发 → 小程序预览和下载
```

转发接口还必须限制：

- 只允许已经验证过的视频源。
- 文件类型和文件大小。
- 请求超时。
- 并发和流量。
- 短期有效，避免被长期盗链。

### 第 4 步：把图片结果迁移到对象存储

本地单用户版本只保留一个：

```text
backend/outputs/latest.png
```

这在本机测试最简单，但多人同时使用时，后一个人的图片会覆盖前一个人的结果。

公网版必须做到：

- 每次请求生成独立、不可猜测的文件名。
- 上传到云存储，而不是依赖容器本地磁盘。
- 设置自动过期，例如 1 小时后删除。
- 不让不同用户拿到彼此的结果。

### 第 5 步：做真机验收

至少用一部没有连接开发电脑的手机检查：

- 分享链接能解析。
- 视频能完整播放。
- 视频能保存到相册。
- 图片能上传。
- 框选坐标准确。
- 修复结果能预览和保存。
- 拒绝相册权限后有易懂提示。
- 第三方接口超时后可以重试。

只有完成这一步，才能说“其他普通用户可以独立使用”。

### 第 6 步：补上最小生产保护

上线前至少还要有：

- 请求频率和并发限制。
- 视频文件大小和流量限制。
- 第三方解析成功率与超时监控。
- 临时文件自动清理。
- 隐私说明和用户授权提示。
- “只处理自己拥有或获授权内容”的使用声明。

账号系统、历史记录、付费套餐、广告和后台管理都可以等真正有用户以后再考虑。

---

## 十六、交给下一位开发者或 AI 的任务单

当云环境开通并拿到环境 ID 后，可以直接使用下面这段：

```text
项目仓库：
https://github.com/liyuan-ohh/Doubao-watermark-remover

当前状态：
本地微信开发者工具已经验证视频解析和图片框选修复。
前端仍访问 http://127.0.0.1:8000，公网部署尚未完成。

已知云信息：
- 环境 ID：<填写控制台真实环境 ID>
- 云托管服务名称：<创建后填写真实名称>

请按最小上线闭环实施：
1. 先读取 README.md、PROJECT_HANDOFF.md、backend/app.py、backend/test_app.py、pages/index/index.js。
2. 保留当前已经验证的解析逻辑，不做无关重构。
3. 增加 CloudBase 云托管所需的最小部署文件，并监听平台 PORT。
4. 先打通 /api/video/parse，再处理视频同域转发。
5. 将图片结果改为独立文件并存入云存储，禁止继续共用 latest.png。
6. 小程序接入 wx.cloud.callContainer 或稳定 HTTPS 服务。
7. 运行现有 5 项测试，并补一项最小的云端调用检查。
8. 最后在真机验证请求、上传、预览和保存。

禁止：
- 添加广告、付费、历史记录、账号或后台管理。
- 把带水印视频作为解析失败时的回退结果。
- 上传 AppSecret、Token、密码和用户素材。
- 把开发者工具测试描述成已经完成真机验收。

回报：
- 修改文件列表。
- 部署地址和版本。
- 测试命令与结果。
- 真机已验证项和仍未验证项。
```

---

## 十七、完整验收清单

### 本地环境

- [ ] Git、Python 和微信开发者工具已安装。
- [ ] 项目从 GitHub 成功下载。
- [ ] `backend/.venv` 已创建。
- [ ] `backend/requirements.txt` 已安装。
- [ ] `http://127.0.0.1:8000/docs` 可以打开。
- [ ] 5 项自动测试全部通过。

### 微信开发者工具

- [ ] 打开的是项目根目录，不是默认示例项目。
- [ ] 本地调试已关闭合法域名校验。
- [ ] 能看到“豆包链接”和“图片去水印”两个模式。
- [ ] 空输入会提示先粘贴链接。
- [ ] 请求期间不会重复提交。
- [ ] 失败后输入仍然保留，可以重试。

### 视频

- [ ] 使用的是自己有权处理的当前有效公开分享链接。
- [ ] 解析后能正常播放。
- [ ] 已检查前段、中段和后段，不只看第一帧。
- [ ] 画面各角落没有移动的“豆包 AI 生成”水印。
- [ ] 解析失败时不会返回带水印视频冒充成功。

### 图片

- [ ] JPEG、PNG 或 WebP 能选择。
- [ ] 可以拖动框选。
- [ ] 过小或越界区域被拒绝。
- [ ] 输出宽高与原图一致。
- [ ] 框选区域改变，远处像素保持不变。
- [ ] 复杂背景效果不佳时会调整框选，而不是承诺完美恢复。

### 公网发布前

- [ ] CloudBase 环境已创建并记录环境 ID。
- [ ] FastAPI 已在云托管运行，不依赖本地电脑。
- [ ] 小程序不再访问 `127.0.0.1`。
- [ ] 视频通过稳定服务域名预览和下载。
- [ ] 图片结果使用独立文件和云存储。
- [ ] 临时文件会自动删除。
- [ ] 已有限流、超时、大小限制和监控。
- [ ] 已完成真实手机测试。
- [ ] 已补隐私和授权说明。

---

## 十八、最后的总结

这次最有价值的并不是写出了多少代码，而是把一个模糊想法变成了可以逐项验收的产品：

1. 先说清楚用户最终要完成什么。
2. 用 PRD 限定第一版范围。
3. 参考现有产品，但不把广告等无关元素当成需求。
4. 研究开源方案，但一定检查许可证和当前可用性。
5. 让 AI 按目录、边界和验收标准工作。
6. 遇到问题先找证据，不靠字段名和页面提示猜结果。
7. 把自动测试、真实接口测试、开发者工具测试和真机测试分开描述。
8. 本地成功后，再解决云部署、多人隔离、成本和隐私。

对小白来说，不需要一开始就懂所有技术。真正需要掌握的是：每次只解决一个明确问题，并且知道怎样证明它真的解决了。

