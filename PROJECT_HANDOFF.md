# 豆包去水印小程序：项目回顾、PRD 与下一阶段交接

更新日期：2026-07-30
项目目录：`D:\Code\微信去水印小程序\Doubao watermark remover`

## 0. 当前结论

第一阶段已经完成并通过真实链接验证：

- 豆包公开 `video-sharing` 分享链接可以解析出无动态水印视频。
- 用户不需要登录豆包。
- 图片支持手动框选水印区域并使用 OpenCV 修复。
- 当前仅适合开发者工具和本机单用户使用。
- 本地后端正在 `http://127.0.0.1:8000` 运行。

当前视频解析依赖 BugPk 第三方服务。后端只接受满足以下条件的结果：

- 第三方业务状态为 `code=200`。
- 返回地址必须是 HTTPS。
- 地址参数必须是唯一的 `lr=unwatermarked`。
- 地址不得包含 `video_gen_watermark`。
- 解析失败时不得回退到带水印视频。

## 1. 主 Agent 如何让执行 Agent 产出内容

采用“主 Agent 管产品与验收，子 Agent 做限定范围实现”的方式。

### 1.1 角色分工

- 主 Agent：
  - 提炼需求和产品边界。
  - 先做只读调查与技术验证。
  - 把任务拆成可独立验收的小任务。
  - 审查子 Agent 的代码，不直接接受“测试通过”等口头结论。
  - 运行自动测试、真实接口测试和微信开发者工具验收。
- 研究 Agent：
  - 检索公开代码、浏览器扩展和当前可用接口。
  - 使用用户提供的真实分享链接验证候选方案。
  - 不修改项目代码。
- 后端执行 Agent：
  - 固定使用 `gpt-5.6-sol / Medium`。
  - 遵循 Ponytail full，优先删除旧依赖和复用现有依赖。
  - 只修改指定后端文件及测试。
- 小程序执行 Agent：
  - 负责第一阶段原生微信小程序页面。
  - 不引入 UI 库，不扩展广告、账户、历史等范围外功能。

### 1.2 给 Agent 的任务结构

任务不是一句“帮我修好”，而是使用下面的固定结构：

1. **身份与模型**：说明执行角色、指定模型和推理级别。
2. **工作目录**：给出唯一绝对路径，避免改错项目。
3. **问题证据**：说明当前接口为什么错误，以及已经验证过什么。
4. **允许改动**：列出可以修改的文件和功能。
5. **禁止事项**：禁止前端扩展、部署、Git、无关重构和新增依赖。
6. **信任边界**：说明必须保留的域名、响应和文件校验。
7. **验收条件**：明确成功、失败、超时及旧功能回归测试。
8. **回报格式**：要求报告修改文件、测试结果和未验证项。

本次后端任务的核心验收语句是：

> `/api/video/parse` 必须返回经过验证的无水印源；无水印解析失败时直接失败，绝不能把豆包带水印地址当作成功结果。

### 1.3 主 Agent 的二次纠偏

子 Agent 第一版虽然取得了无水印地址，但仍依赖旧的豆包页面 VID 提取和
`get_play_info` 元数据接口。主 Agent 审查后指出：

- 已经得到无水印地址时，不应因为旧元数据接口失败而让核心功能失败。
- BugPk 已经能够直接处理分享链接，继续保留旧链路只会增加故障点。

随后让子 Agent 删除旧页面读取、VID 提取和豆包元数据请求。最终
`/api/video/parse` 只保留“校验分享链接 → 调用 BugPk → 校验无水印地址”这一条路径。

## 2. 第一阶段 PRD

### 2.1 产品目标

提供一个干净、简单的微信小程序，让用户完成两件事：

1. 粘贴豆包分享文案或链接，取得无水印视频并保存。
2. 上传带水印图片，手动框选水印区域，修复并保存图片。

### 2.2 目标用户

- 第一阶段：开发者本人，在微信开发者工具中使用。
- 第二阶段：普通微信用户，无需安装 Python、无需启动本地服务。

### 2.3 核心用户故事

#### 豆包视频

作为用户，我粘贴别人发给我的豆包公开分享链接，点击“一键解析”，就能预览并保存无动态水印视频；我不需要登录豆包。

#### 图片修复

作为用户，我选择一张图片，在图片上拖动框选水印，点击修复后可以预览和保存结果。

### 2.4 第一阶段功能范围

- 单页双模式：豆包链接、图片去水印。
- 链接输入、粘贴、清空、解析。
- 视频预览和保存。
- 图片选择、拖动框选、重新框选、修复、预览和保存。
- 请求期间禁用重复提交。
- 失败时保留输入和框选结果，允许重试。
- 图片限制：JPEG、PNG、WebP；15MB；2500 万像素。
- 框选区域最小 `8×8` 像素且不得越界。

### 2.5 明确不做

- 广告、账户、数据库、历史记录。
- 批量处理、自动识别图片水印。
- 付费、后台管理、教程页。
- 视频逐帧修复。
- 第一阶段云部署和真机发布。

### 2.6 第一阶段验收

- 非豆包域名被拒绝。
- 第三方无源、带水印地址和超时均返回易懂错误。
- 合规地址必须为 `lr=unwatermarked`。
- 真实分享链接在本地 FastAPI 路由返回 200。
- 抽查视频第 2、6、10、14 秒，无移动的“豆包AI生成”水印。
- 图片修复保持原尺寸；框内像素改变，远离框选区域的像素不变。
- 自动测试 5/5 通过。
- 用户在微信开发者工具中确认视频已经无水印。

## 3. 开发中遇到的问题

### 3.1 微信开发者工具最初显示空白或默认 Weixin 页面

原因：开发者工具打开了错误的示例项目或错误目录，不是实际项目根目录。

处理：重新打开
`D:\Code\微信去水印小程序\Doubao watermark remover`，确认资源管理器中同时存在
`backend`、`pages`、`app.js` 和 `project.config.json`。

### 3.2 “原视频地址”实际仍有动态水印

原因：旧代码读取豆包
`/samantha/media/get_play_info` 的 `original_media_info.main_url`。这个字段现在返回的
CDN 文件已经把动态水印烧进像素，并不等于无水印源。

### 3.3 修改 URL 参数无效

曾尝试把链接中的水印参数替换为 `video_gen_no_watermark` 或
`unwatermarked`，但下载文件的大小和 SHA-256 完全相同。

结论：仅修改查询参数不会切换 CDN 对象，必须取得另一份真实源文件。

### 3.4 豆包内部无水印接口涉及登录态

部分公开脚本通过已登录豆包网页取得 `fallback_api`，再解密 qAAB 视频地址。
这条路线依赖服务端维护 Cookie，不适合让普通小程序用户自己登录，也不适合当前最短实现。

这里的“登录态”只可能属于解析服务后台，不是最终用户的使用条件。

### 3.5 第一版修复仍被旧元数据链路拖累

第一版虽然先取得无水印 URL，但之后仍请求旧豆包接口获取封面和尺寸。旧接口一旦失败，
整个解析仍失败。

处理：删除旧豆包页面、VID 和元数据请求。封面和尺寸不是核心成功条件，视频组件可以直接
读取无水印源。

### 3.6 重新编译小程序后仍显示旧水印

原因：微信开发者工具只重新编译小程序前端，不会重启独立运行的 Python 后端。
端口 8000 上的旧进程仍加载旧代码。

处理：停止旧 Uvicorn 进程并重新启动后端，然后重新点击“一键解析”。

## 4. 最终解决方案

当前调用流程：

```text
用户粘贴豆包分享链接
        ↓
小程序 POST /api/video/parse
        ↓
本地 FastAPI 校验 doubao.com 域名和允许的分享路径
        ↓
FastAPI 调用 BugPk 解析服务
        ↓
校验 code、HTTPS、lr=unwatermarked、无水印标记
        ↓
小程序预览并保存无水印视频
```

关键实现：

- `backend/app.py`：视频解析、结果校验和图片修复。
- `backend/test_app.py`：非法域名、成功、无源、带水印、超时和图片修复测试。
- `backend/THIRD_PARTY_NOTICES.md`：BugPk 运行时依赖声明。
- `pages/index/index.js`：当前本地接口地址和交互逻辑。

## 5. 如何重新启动本地后端

### 5.1 正常启动

打开 PowerShell，执行：

```powershell
Set-Location "D:\Code\微信去水印小程序\Doubao watermark remover"
.\backend\.venv\Scripts\python.exe -m uvicorn backend.app:app --host 127.0.0.1 --port 8000
```

保持这个 PowerShell 窗口打开。看到 Uvicorn 正在监听后，再在微信开发者工具中点击
“一键解析”。

停止服务时，在这个窗口按 `Ctrl+C`。

### 5.2 端口 8000 已被旧后端占用

先只停止命令行中确认为本项目 Uvicorn 的进程：

```powershell
$listener = Get-NetTCPConnection -LocalPort 8000 -State Listen -ErrorAction SilentlyContinue | Select-Object -First 1
$process = if ($listener) { Get-CimInstance Win32_Process -Filter "ProcessId = $($listener.OwningProcess)" }
if ($process -and $process.CommandLine -match "uvicorn\s+backend\.app:app") {
    Stop-Process -Id $process.ProcessId
}
```

然后重新执行 5.1 的启动命令。

### 5.3 验证服务

浏览器打开：

```text
http://127.0.0.1:8000/docs
```

能够看到 FastAPI 接口文档，说明后端已经启动。微信开发者工具仍需关闭合法域名校验，
因为本地地址不是正式小程序合法域名。

## 6. 第二阶段 PRD：面向其他用户

### 6.1 阶段目标

普通用户打开正式小程序即可使用，不安装 Python、不连接开发者电脑、不手动启动后端。

### 6.2 推荐的最短架构

```text
微信小程序
   ↓ 微信云托管调用或稳定 HTTPS 域名
CloudBase 云托管中的 FastAPI
   ├─ 豆包分享链接校验与 BugPk 解析
   ├─ 通过自有服务域名转发视频
   └─ OpenCV 图片修复 → 临时对象存储
```

推荐腾讯云 CloudBase 云托管的原因：

- 官方支持把现有 FastAPI 部署为容器服务。
- 小程序可以通过 `wx.cloud.callContainer` 访问云托管服务。
- 使用云托管私有链路时，无需在小程序后台配置普通 request 合法域名。
- 如继续使用 `wx.uploadFile`、`wx.downloadFile` 和 `<video>`，也可以开放一个稳定的
  HTTPS 公网域名并配置为小程序合法域名。

官方参考：

- [CloudBase FastAPI 部署](https://docs.cloudbase.net/run/develop/languages-frameworks/fastapi)
- [微信小程序访问云托管](https://docs.cloudbase.net/run/develop/access/mini)
- [云托管服务和公网域名](https://docs.cloudbase.net/run/deploy/service-setting)
- [downloadFile 域名配置](https://docs.cloudbase.net/lowcode/practices/miniapp-guide/downloadfile-guide)

### 6.3 第二阶段必须完成的改动

#### P0：上线闭环

1. 创建并关联 CloudBase 环境。
2. 为现有 FastAPI 增加最小容器部署文件。
3. 将 `API_BASE = "http://127.0.0.1:8000"` 改为生产环境调用方式。
4. 视频不再把变化的第三方 CDN 域名直接交给小程序：
   - 后端生成短期有效的签名转发地址。
   - 小程序始终从自己的稳定域名预览和下载。
5. 图片结果不再共用 `latest.png`：
   - 每次请求生成不可猜测的独立文件 ID。
   - 上传到对象存储。
   - 设置自动过期和删除时间。
6. 在真机验证 request、uploadFile、video 和 downloadFile。
7. 补充隐私说明、用户授权提示和“只处理有权使用的内容”声明。

#### P1：最小生产保护

1. 按 OpenID 或 IP 限制解析频率和并发。
2. 保留现有 15MB、2500 万像素和框选边界校验。
3. 给 BugPk 请求增加监控、超时和失败统计。
4. 对视频转发设置文件大小、媒体类型、超时和流量上限。
5. 日志不得记录完整的长期用户标识和图片内容。

#### P2：有真实增长后再做

- 多解析供应商自动切换。
- 付费、套餐和后台管理。
- 历史记录和用户账户。
- 视频对象存储缓存。
- 自动识别图片水印或重型修复模型。

这些功能在没有真实用户量和成本数据前不应提前开发。

### 6.4 上线前需要产品负责人决定

1. 使用微信云托管，还是自有云服务器与备案域名。
2. 预计日活和每天解析视频数量。
3. 单个视频约数十 MB，是否接受预览和保存产生的带宽费用。
4. 图片和视频结果保留多久，建议默认 1 小时后自动删除。
5. BugPk 作为生产依赖是否可接受；如不可接受，需要维护服务端豆包账号链路或购买正式解析服务。
6. 小程序主体、AppID、云环境和费用账户由谁提供。

### 6.5 第二阶段完成标准

- 一部未连接开发电脑的真机可以独立使用两项核心功能。
- 用户不需要启动本地后端，也不需要登录豆包。
- 小程序请求、上传、预览和下载全部经过已配置的稳定服务域名。
- 两名用户同时操作时，图片结果不会互相覆盖。
- 结果文件按约定时间自动删除。
- 第三方解析故障时显示可重试提示，不返回带水印视频。
- 能看到请求量、成功率、第三方失败率和带宽用量。

## 7. 下一位开发者的开工顺序

1. 先读取本文件、`backend/app.py`、`backend/test_app.py` 和
   `pages/index/index.js`。
2. 不改解析算法，先完成 CloudBase 环境与 FastAPI 最小部署。
3. 打通小程序到云端 `/api/video/parse`。
4. 实现同域视频签名转发，真机完成预览和保存。
5. 把图片结果迁移到对象存储并做用户隔离。
6. 最后补限流、日志、隐私和清理策略。

下一阶段不应先做广告、付费、历史或后台；先让一台完全独立的真机跑通核心闭环。
